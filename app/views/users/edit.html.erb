<%= form_for @user, :as => user, :url => account_path, :html => {:class => "mainForm clear", :id => "formEditor", :multipart => true} do |u| %>
  <p id="asterisk"><span class="star">*</span> Campos obrigatórios.</p>
  <fieldset>
    <legend><span>Edição</span></legend>
    <%= render :partial => "users/partials/form"%>
  </fieldset>
  
  <div class="formActions clear">
    <%= f.submit "Alterar Cadastro", :class => "button primary"
  </div>
<%end%>