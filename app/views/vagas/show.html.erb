<!-- start main -->
<div id="main" class="clear">

  <%= raw show_msg(notice, :success) %>

  <div id="head" class="clear">
    <h1><%= @vaga.codigo %></h1>
  </div>
  <div class="textContent full clear">
    <p>Código: <%= @vaga.codigo %></p>
    <p>Mapa: <%= @vaga.map.nil? ? "Sem mapa associado." : @vaga.map.codigo %></p>
    <p><%= raw vaga_status(@vaga) %></a></p>
    <p>Tipo de vaga: <%= @vaga.especial ? "Reservada para deficientes físicos" : "Normal" %></a></p>
    
  </div>

</div>
<!-- end main -->

<!-- start sidebar -->
<div id="sidebar">
  <!-- start context help sidebox -->
  <div id="help" class="sidebox">
    <h1>Ferramentas</h1>

    <div class="boxContent clear">
      <%= link_to 'Editar', edit_vaga_path(@vaga.codigo) %> |
      <%= link_to 'Voltar', vagas_path %>
    </div>
  </div>
  <!-- end context help sidebox -->
</div>
<!-- end sidebar -->