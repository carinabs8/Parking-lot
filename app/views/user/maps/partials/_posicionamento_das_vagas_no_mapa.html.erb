<%=image_tag map.photo.url(:small), :id => "map_image"%>
<% map.vacancies.each do |vacancy| %>
  <%unless (vacancy.eixo_x.blank? || vacancy.eixo_y.blank?) %>
    <%= link_to_add_vacancy(vacancy)%>
  <% end %>
<% end %>